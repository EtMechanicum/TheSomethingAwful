#Recipe fields messages
recipe.title=introduce a title for the recipe
recipe.description=the recipe must have a description
recipe.steps=at least one step is required
recipe.ingredients=at least one ingredient is required
recipe.preparationTime=set a preparation time
recipe.difficulty=select a difficulty level for the recipe

#Review fields messages
review.title=the review must have a title
review.body=the review's body must not be blank
#Review.rating has no need of constraints since the possible values are already set 

#Credentials fields messages
NotBlank.credentials.username=username field cannot be blank
Size.credentials.username=username must be at least 8 characters long
NotBlank.credentials.password=your password cannot be blank
Size.credentials.username=your password must be at least 8 characters long

